# Copy this file to tests/e2e/.env and fill in your values.
# tests/e2e/.env is gitignored — never commit tokens.
MATOMO_URL=https://matomo.kreativhub.tech
MATOMO_SITE_ID=5
MATOMO_TOKEN=
# Optional: attach to a running example dev server instead of booting one
# EXAMPLE_URL=http://localhost:3000
# EXAMPLE_PORT=3100
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable
